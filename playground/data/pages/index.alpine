<script server lang="ts">
  export function loader() {
    return { title: 'Apex Data' }
  }
</script>

<template x-data>
  <main>
    <h1 x-text="title"></h1>
    <p>
      <code>todos</code> is a REST + MCP resource from one definition.
      Try <code>GET /api/todos</code> — or point an MCP client at <code>/mcp</code>.
    </p>
  </main>
</template>

<style scoped>
  main { max-width: 40rem; margin: 3rem auto; font-family: system-ui, sans-serif; }
  h1 { color: #2563eb; }
</style>
