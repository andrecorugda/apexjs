// apexjs/client — the browser runtime, re-exported so user apps that depend on
// `apexjs` can import it without needing `apexjs-kit` as a direct dependency.
export { registerApexComponent } from 'apexjs-kit/client'
