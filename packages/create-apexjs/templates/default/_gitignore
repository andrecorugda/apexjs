node_modules
dist
*.log

# Local env — keep .env.example committed, never real secrets
.env
.env.local
.env.*.local

# Local SQLite database (schema lives in db/migrations/, not the file)
*.db
*.db-journal
*.db-wal
*.db-shm

# Native mobile build artifacts (the shell is regenerated by `apex mobile <platform>`)
# local.properties holds a machine-specific SDK path; never commit it.
mobile/android/local.properties
mobile/**/build/
mobile/**/.gradle/
*.apk
*.aab
*.ipa
