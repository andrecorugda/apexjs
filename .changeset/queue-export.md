---
"@apex-stack/core": minor
---

Background job queue exported from `@apex-stack/core/server` (`createQueue`, `defineJob`, memory
+ database drivers with retries/backoff, delayed jobs, and a poll `work()` loop).
