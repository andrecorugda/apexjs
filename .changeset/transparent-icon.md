---
"@apex-stack/core": patch
---

VS Code extension: transparent (backgroundless) icon. The bundled `.alpine` extension icon is now a proper transparent-background glyph (the faceted Apex "A" mark) instead of a solid-background image. Extension bumped to 0.1.1 and re-bundled into the package.
